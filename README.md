# SH4GEN Static Site (Modular)

- `index.html` — ana HTML entry
- `css/style.css`
- `js/app.jsx` — React uygulaması (CDN React + Babel)
- `CNAME` — hedef GitHub Pages URL: `sh4gen.github.io`

Yerel test:

```powershell
# Windows: açmak için
start .\index.html
```
